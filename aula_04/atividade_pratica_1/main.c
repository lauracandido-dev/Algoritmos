// Escreva um programa que use um laço for para imprimir os
// números de 1 a 10, cada um em uma linha separada.

#include <stdio.h>

int main(){
    for(int i = 1;i < 11;i++ ){
        printf("%d\n", i);
    }
    return 0;
}